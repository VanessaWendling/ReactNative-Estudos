import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        borderColor: "#F0001A",
        borderWidth: 2,
        padding: 12,
        flexDirection: 'column',
        width: 150,
        alignItems: 'center',
        borderRadius: 5
    }
});