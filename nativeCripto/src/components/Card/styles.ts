import { StyleSheet } from 'react-native';



export const styles = StyleSheet.create({
    container: {
        borderWidth: 2,
        borderRadius:15,
        paddingVertical: 10,
        justifyContent: 'space-evenly',
        flexDirection:'row'
        
    }
});