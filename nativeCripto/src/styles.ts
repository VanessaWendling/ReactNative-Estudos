import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    header: {
        backgroundColor: '#49aff7',
        paddingVertical: 20,
        alignItems: 'center',
        marginBottom: 5
    },
    title: {
        fontSize: 30,
        color: '#fff'
    }
});